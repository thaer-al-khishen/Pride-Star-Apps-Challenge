package com.example.pridestarappschallenge.network.helpers.helpers

import com.example.pridestarappschallenge.BuildConfig

object UrlBuilder {
    const val BASE_URL = BuildConfig.baseUrl

    //home
    const val GET_ALL_HOME = "home.json"
}